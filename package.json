{
    "name": "streamdeck-ai-agent-monitor-root",
    "private": true,
    "engines": {
        "node": ">=20.0.0",
        "pnpm": ">=9.0.0"
    },
    "scripts": {
        "build": "pnpm -r run build",
        "test": "pnpm -r run test",
        "lint": "pnpm -r run lint",
        "stage:plugin": "node scripts/stage-plugin.mjs",
        "package:plugin": "pnpm -r run build && node scripts/stage-plugin.mjs && pnpm --package=@elgato/cli dlx streamdeck validate dist/com.antigravity.aimonitor.sdPlugin && pnpm --package=@elgato/cli dlx streamdeck pack dist/com.antigravity.aimonitor.sdPlugin --output dist"
    },
    "devDependencies": {
        "typescript": "^5.4.0",
        "zod": "^3.23.0"
    }
}
